# Local machine config (paths + secrets) — never commit.
# Anchored to repo root so lib/config.sh (the shared loader) is still tracked.
/config.sh

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
